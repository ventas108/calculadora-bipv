# Registro de decisiones

| Fecha | Decisión | Motivo | Módulo(s) afectados |
|---|---|---|---|
| 2026-09-14 | Adoptar arquitectura híbrida: director central + Specs modulares en `CodeSpecs/` | Evitar Specs gigantes y descoordinación entre módulos; permitir trabajar con Claude y Copilot sin duplicar esfuerzos; ganar trazabilidad y validación módulo por módulo | Todos |
| 2026-09-15 | Aprobar `problema.md`, `propuesta.md` y `diseno.md` de `01-datos-proyecto`; corregir regla de precedencia de tarifa por ciudad (no sobreescribir un valor editado manualmente al cambiar de ciudad) | Los valores del catálogo son una aproximación regional; el usuario los corrige con su factura real y esa corrección se perdía silenciosamente al cambiar de ciudad | `01-datos-proyecto`, `06-analisis-financiero` |
| 2026-09-15 | Cerrar `01-datos-proyecto` como `completado`: implementación desplegada en producción y verificación funcional confirmada (valor manual conservado, aviso correcto, botón de aplicar sugerencia funcional) | Primera Spec vertical llevada de principio a fin por el ciclo SDD completo (problema → propuesta → diseño → aprobación → tareas → implementación por Claude Code → validación) | `01-datos-proyecto` |
| 2026-09-16 | Aprobar `problema.md`, `propuesta.md` y `diseno.md` de `02-recurso-solar/react`; unificar el cálculo mensual de POA y reforzar pruebas, manteniendo fuera IAM/soiling, producción, PR_T y PVGIS ya resuelto | La duplicación entre `Home.tsx` y `POAAnalyzer.tsx` permite divergencias silenciosas; faltan pruebas específicas del modelo, parser y proxy real | `02-recurso-solar/react`, `04-produccion-energia`, `05-perdidas-y-temperatura` |
| 2026-09-16 | Formalizar como política vigente la separación operativa y técnica entre la app web principal (`calc.innovacionquimica.com.co`, `/var/www/bipv/calculadora`, pm2 `calculadora-bipv`) y la app hermana Streamlit (`bipv.innovacionquimica.com.co`, `/var/www/bipv/calculadora-bipv`, pm2 `streamlit-bipv`) — ver [`separacion-apps.md`](separacion-apps.md) | Evitar despliegues cruzados, confusión de entornos (rutas casi idénticas) y modificaciones no autorizadas en producción; todo cambio, incluida documentación, debe declarar explícitamente a cuál app afecta | Todos |
| 2026-09-19 | Cerrar retrospectivamente `03-dimensionamiento` y exigir que Producción bloquee simulaciones con diseño eléctrico vencido | Producción ya consumía el estado confirmado, pero un aviso de vigencia no impedía persistir energía con panel/inversor distintos al diseño confirmado; se añadió gate y prueba de regresión | `03-dimensionamiento`, `04-produccion-energia` |
| 2026-09-19 | Distinguir orden de fases de dependencia de runtime entre Producción y Motor Óptico | El Motor Óptico (`05`) publica POA sin térmico y parámetros térmicos que Producción (`04`) debe consumir para evitar doble conteo; la hoja de ruta conserva `04` antes de `05`, pero el contrato de datos es `05 → 04` | `04-produccion-energia`, `05-perdidas-y-temperatura` |
| 2026-09-19 | Exigir firma determinista para reutilizar o restaurar resultados de Producción | Evitar que energía, bypass o agregados persistidos sobrevivan cambios de panel, inversor, POA o pérdidas; Finanzas y Presupuesto deben reconstruir la firma antes de restaurar en otra pestaña | `04-produccion-energia`, `05-perdidas-y-temperatura`, `06-analisis-financiero` |
| 2026-09-19 | Aprobar diseño de `06-analisis-financiero`: persistir el payload canónico de `produccion_run_signature_v1` junto a la firma y verificar su integridad al restaurar, en vez de reconstruir la firma desde `session_state` (inviable: Finanzas/Presupuesto no cargan panel/TMY/POA en pestaña nueva) | Hallazgo de exploración: sin este ajuste, la restauración en pestaña nueva quedaría permanentemente bloqueada por falta de insumos, no por diseño | `04-produccion-energia`, `06-analisis-financiero` |
| 2026-09-19 | Cerrar `06-analisis-financiero` como completado: implementación desplegada (`ccd60c29`), validación funcional confirmada en producción (pestaña nueva restaura con aviso y datos reales) | Cierra el ciclo SDD completo del módulo; de paso se corrigió un bug no relacionado en la tabla de flujo de caja de Financiero (`81f5279b`), hallado durante la validación manual | `06-analisis-financiero` |
| 2026-09-19 | Cerrar `07-informes` como completado sin cambios de código: la exploración confirmó que Reporte PDF, Baterías/Balance e Impacto CO₂ no restauran resultados por su cuenta y ya heredan la vigencia garantizada en `04`/`06` | Evitar una "corrección" innecesaria donde no había gap real; formaliza el contrato para detectar a futuro si alguna página de Informes agrega restauración propia | `07-informes` |
| 2026-09-19 | Cerrar `04-produccion-energia` y `05-perdidas-y-temperatura` como completado, con `38 passed` de pruebas físicas frescas (pérdida óhmica, balance PVsyst, coherencia térmica bypass) | Últimos ítems pendientes de ambos módulos ya tenían evidencia real disponible; el bloqueo de Finanzas quedó resuelto por diseño (verificación de payload) en `06`, no por reconstrucción de firma | `04-produccion-energia`, `05-perdidas-y-temperatura` |
| 2026-09-19 | Cerrar `08-interfaz` como completado sin cambios de código: la exploración confirmó que `utils/ui.py` y el patrón común de página no restauran ni invalidan datos, y el escapado HTML de texto de usuario ya estaba resuelto | Mismo criterio que `07-informes`: evitar una "corrección" innecesaria donde no había gap real; formaliza el contrato de página compartido | `08-interfaz` |
| 2026-09-19 | Corregir ruta y nombre de proceso PM2 en `actualizar_en_servidor.sh`, `instalar_servidor.sh` y consolidar `ecosystem.config.cjs` como fuente única de la app Streamlit | Los scripts usaban `/var/www/bipv/calculadora_bipv` y `calculadora-bipv-python`, que nunca coincidieron con la realidad (`/var/www/bipv/calculadora-bipv`, proceso `streamlit-bipv`); ejecutarlos tal cual habría fallado | `09-despliegue` |
| 2026-09-19 | Formalizar los comparadores de paneles, inversores y orientación como superficies de decisión exclusivas de Streamlit que consumen los motores y contratos vigentes; separar además el Asistente general de los Analistas locales e incluir `separacion-apps.md` en el contexto obligatorio del agente SDD | Evitar que una comparación cree una segunda física, adopte datos distintos de los simulados, conserve derivados obsoletos o se replique accidentalmente en React. El asistente orienta y los analistas interpretan tablas actuales, pero ninguno modifica el proyecto; el agente SDD debe conocer esa frontera antes de preparar cambios | `02-recurso-solar`, `03-dimensionamiento`, `04-produccion-energia`, `05-perdidas-y-temperatura`, `06-analisis-financiero`, `07-informes`, `08-interfaz` |
| 2026-09-19 | Registrar como desviaciones activas la conservación parcial del estado óptico al adoptar inversor, la adopción global de orientación en modo multi-superficie y la falta de firma común para tablas/análisis de comparadores | La revisión del contrato contra el código mostró que esas garantías aún no son completas. El Director debe exponer la deuda y exigir Specs verticales, no describir como vigente un comportamiento aspiracional | `03-dimensionamiento`, `04-produccion-energia`, `05-perdidas-y-temperatura`, `08-interfaz` |
| 2026-09-20 | Resolver la desviación de conservación óptica al adoptar inversor mediante una transición central e idempotente que conserva las 15 claves del Motor Óptico e invalida los resultados eléctricos y downstream | Cambiar inversor o `N_serie` no cambia TMY, geometría, panel, IAM, soiling ni NOCT. La exclusión anterior conservaba solo `poa_efectiva_df` y podía hacer que Producción cayera silenciosamente a POA bruta; la nueva transición mantiene también `poa_sin_termico_df`, estado, resumen y parámetros ópticos | `03-dimensionamiento`, `04-produccion-energia`, `05-perdidas-y-temperatura`, `06-analisis-financiero`, `08-interfaz` |
| 2026-09-21 | Evaluar `pybdshadow` (librería externa BSD-3) como proveedor de `p_shade` y descartarlo como proveedor universal o eléctrico; como máximo, referencia algorítmica opcional para cubiertas horizontales sin malla 3D propia | Verificación técnica con prototipos ejecutados (`references/informe-verificacion-pybdshadow-p-shade.md`): su motor geométrico es correcto solo para planos horizontales (sin parámetro de tilt/azimuth en su API), error de hasta 51 puntos porcentuales si se fuerza en fachadas/superficies verticales o inclinadas; no calcula nada eléctrico. No resuelve el bloqueo real de sombra por superficie de `transicion-multisuperficie`, que es sobre fachadas/inclinadas | `05-perdidas-y-temperatura` |
| 2026-09-21 | Decidir Motor Óptico GLOBAL (opción A) para la primera versión del modo físico multi-superficie, sin activar Motor Óptico por superficie; ratifica la arquitectura de invalidación ya vigente en vez de abrir una Spec nueva de alcance mayor | `calculos/invalidacion.py` ya trata `KEYS_MULTISUP_ESTADO` como un ciclo de vida independiente del Motor Óptico (POA propia, `KEYS_DOWNSTREAM_MOTOR_OPTICO` la excluye explícitamente desde el 17-sep-2026) — Motor Óptico por superficie exigiría rediseñar esa invalidación, cambio de alcance mayor no justificado todavía. `transicion_multisuperficie.recalcular_fisica_superficie` ya separa explícitamente POA geométrica (pvlib bruta, `calcular_poa_superficie`) de POA óptica (Motor Óptico global, `poa_sin_termico_df`, opt-in por superficie vía `motor_optico_vigente`), pérdida térmica (una sola vez, dentro de `simular_bypass_horario`) y sombra (`p_shade`, eje independiente) — la primera versión del modo físico multi-superficie opera SIN IAM/soiling por superficie (cada superficie usa POA cruda de pvlib salvo que declare explícitamente `motor_optico_vigente=True` con `poa_sin_termico_df` real, ya bloqueado sin default silencioso por `recalcular_fisica_superficie`) | `05-perdidas-y-temperatura` |
| 2026-09-21 | Cerrar parcialmente el bloqueo "sombra por superficie" de `transicion-multisuperficie`: `calculos.sombras_3d.calcular_fs_horario_por_superficie` ahora distingue sombra-cero-calculada de cálculo-incompleto y de error-geométrico, y advierte cuando la resolución espacial (puntos de análisis) es menor que `N_serie`; se agregan `calculos/vinculador_sombra_multisuperficie.py` (frontera pura sombra↔`superficies_bipv`, con invalidación automática de `p_shade`/`firma_sombra` al cambiar tilt/azimuth) y `calculos/inversores_multisuperficie.py` (validación/derivación de inversor dedicado/compartido). Sigue SIN cerrarse: no existe todavía UI en `pages/9_🗺️_Vista_3D.py` para capturar puntos de análisis por superficie ni malla por superficie, y la UI de asignación de inversor por superficie sigue bloqueada por falta de mockup aprobado (punto 6 de `diseno.md`) | Backend puro y probado primero, UI después: permite validar el contrato físico completo (sombra→POA→SDM/bypass→inversor→agregados) con pruebas unitarias antes de comprometer una UI que depende de decisiones de producto (mockup) todavía no aprobadas | `05-perdidas-y-temperatura` |
| 2026-09-21 | Corregir 5 hallazgos bloqueantes de una auditoría sobre la ronda anterior (mismo día): (1) `tmy_fingerprint` firmaba un vector de ceros en vez de `T2m` real, así que ninguna firma de sombra podía pasar jamás `_validar_firma_sombra` — corregido para exigir el TMY real; (2) sombra incompleta/errónea solo advertía, nunca bloqueaba — se agregó un estado explícito por superficie con bloqueo real en dos capas independientes; (3) el botón "Adoptar" publicaba el candidato guardado sin revalidar — se unificó en una sola función que revalida contra el `session_state` actual antes de adoptar; (4) `inversores_multisuperficie.py` existía pero no estaba conectado a ningún flujo — ahora `construir_proyecto_desde_session_state` lo ejecuta siempre; (5) la invalidación geométrica solo miraba tilt/azimuth — se amplió a todas las entradas reales de la sombra más una invalidación separada por cambio de TMY. Batería focal `38 passed` original pasa a `116 passed`; regresión completa del proyecto sin fallos nuevos (ver `references/correccion-auditoria-multisuperficie.md`) | La implementación anterior parecía completa por sus propias pruebas (38 passed), pero esas pruebas nunca ejercitaron la validación cruzada real entre módulos (firma de sombra vs. validación de la transición, adopción vs. revalidación, inversores vs. flujo real) — los 5 hallazgos son bugs de integración reales, no estilo ni cobertura cosmética | `05-perdidas-y-temperatura` |
| 2026-09-21 | Publicar la primera integración opt-in multi-superficie en `main` y DigitalOcean (`c0caa2136`), manteniendo el modelo simplificado como default | Vista 3D carga Site Designer, recibe puntos `x,y,z` por superficie, valida inversores y permite comparar/adoptar el resultado físico solo después de revalidar el estado actual. La UI de puntos es manual y el Motor Óptico sigue global; la Spec continúa en validación operativa | `05-perdidas-y-temperatura`, `08-interfaz`, `09-despliegue` |
| 2026-09-21 | Adoptar persistencia canónica firmada para el estado físico multi-superficie, con restauración diferida hasta verificar el TMY y limpieza completa al cambiar de proyecto | `persistencia_multisuperficie` separa entradas, resultados, firmas y metadata de proveedores; Cargar no publica `multisup_*` antes de validar el TMY, la geometría, sombra, POA y configuración eléctrica. Un payload inválido o un cambio de proyecto no puede dejar vivo estado físico anterior. El modelo simplificado continúa siendo default y la restauración es todo-o-nada | `05-perdidas-y-temperatura`, `06-analisis-financiero`, `07-informes`, `08-interfaz` |
| 2026-09-24 | Aprobar `problema.md`, `propuesta.md` y `diseno.md` de `05-perdidas-y-temperatura/sombra-cara-trasera`: `FS_geometrico = 0` cuando el sol está detrás del plano del módulo (`sol · normal ≤ 0`), orientación opcional en `calcular_fs_horario` (sin ella, comportamiento previo con advertencia), orientación obligatoria en el flujo por superficie, `VERSION_ALGORITMO_FS_POR_SUPERFICIE` a `v2` e invalidación de sombras persistidas de `sombras_3d` con versión anterior | El ray-casting marcaba como sombra horas con el sol detrás del plano (100 % de la sombra en una torre convexa aislada; 83 % en la escena sintética de cierre), y el bypass recortaba con ello la irradiancia difusa real. Quedan fuera: difusa en el bypass, cadena de pérdidas/PR, base climática y envío de orientación desde React | `05-perdidas-y-temperatura`, `04-produccion-energia` |
| 2026-09-24 | Corregir el `ValueError` del mapa de calor POA de Vista 3D (`08-interfaz/heatmap-poa-superficie`): elegir la serie con `is None` en vez de `or` | Con POA por superficie calculado, `poa_superficies.get(nombre) or poa_df` evaluaba la verdad de un DataFrame y Streamlit cortaba la página (reportado en producción). Se agrega una prueba AST que impide reintroducir `or` sobre series POA en la página | `08-interfaz` |
| 2026-09-24 | Publicar `docs/MANUAL_VISTA_3D.md` y actualizar la sección «8b. Página 9» del Asistente contra el código de `main` `8f212e64` | El Asistente tenía nombres de sub-pestañas, tipos/rangos de superficie y botones desactualizados, y no conocía la sombra v2 ni la corrección del mapa de calor. Se documentan las precauciones verificadas en el código: tres botones escriben `E_ac_anual_kWh_multisup` (gana el último), POA por superficie no se invalida al editar la geometría, puntos a más de 10 cm de la malla, panel por defecto del bypass. Subsecciones < 4.000 caracteres (límite de lectura del Asistente) y 3 pruebas de recuperación nuevas | `08-interfaz`, Asistente |
| 2026-09-24 | Aprobar e implementar `05-perdidas-y-temperatura/vigencia-poa-superficie`: POA por superficie indexada por `uid` y firmada (geometría, montaje, albedo, bifacial, TMY y ubicación); los consumidores de Vista 3D solo usan POA vigentes y avisan qué superficie recalcular; los fallos de cálculo se muestran con su causa | La POA quedaba por nombre y sin firma: tras editar la geometría o cambiar el TMY se seguía mostrando e integrando la POA anterior, y un fallo de pvlib se convertía en una POA vacía. `poa_superficies` no entra en `KEYS_DERIVADOS_POA` (también se aplica al cambiar de inversor); el cambio de sitio lo detecta la firma | `05-perdidas-y-temperatura`, `08-interfaz` |
| 2026-09-24 | Aprobar e implementar `05-perdidas-y-temperatura/publicacion-energia-multisuperficie`: una sola función publica total, desglose, área y POA ponderada con `multisup_origen` (`simplificado`, `bypass_csv`, `fisico`); reemplazar otro origen pide confirmación; en origen físico se publica `multisup_perdida_bus_kWh` | Tres botones escribían la energía cada uno a su manera (el bypass cambiaba el total sin desglose, área ni POA) y el proyecto físico sobrevivía a publicaciones de otro origen y se guardaba con ellas. Financiero, Baterías, CO₂, Reporte y Unifilar siguen leyendo las mismas claves | `05-perdidas-y-temperatura`, `06-analisis-financiero`, `08-interfaz` |
| 2026-09-24 | Aprobar e implementar `08-interfaz/puntos-3d-validacion`: parser de puntos 3D con errores por línea (acepta `x,y,z` y `x;y;z` con coma decimal), vista previa con `validar_puntos` antes de calcular y puntos indexados por `uid` | Las líneas mal escritas o con coma decimal se descartaban en silencio y renombrar una superficie desvinculaba sus puntos. El texto del recuadro se inicializa una vez para que Streamlit no borre las líneas con error | `08-interfaz` |
| 2026-09-24 | Aprobar e implementar `08-interfaz/estado-sombra-superficie`: tabla de estado de sombra por superficie con las mismas reglas que el modo físico, motivos conservados y corrección de la pérdida del resultado de sombra en el mismo rerun | La página no mostraba ningún estado de sombra; además el resultado de «🌳 Calcular sombra» se sobrescribía con la lista previa, por lo que el modo físico decía «falta p_shade» aunque se hubiera calculado | `08-interfaz`, `05-perdidas-y-temperatura` |
| 2026-09-24 | Aprobar e implementar `08-interfaz/panel-proyecto-bypass-mppt`: bypass y MPPT por superficie usan por defecto el panel del proyecto (con SDM completo) y los strings de cada superficie, con origen visible | Ambos arrancaban con `ASP-ST1-T40` y 8 módulos en serie ajenos al proyecto, y el bypass publica energía oficial. Se retiran los N en serie comunes: los strings se editan solo en ⚙️ Superficies BIPV | `08-interfaz`, `06-analisis-financiero` |
| 2026-09-24 | Publicar la rev. 4 del manual de Vista 3D (`docs/MANUAL_VISTA_3D.md` y Word) contra `main` `72326f2f` | Tras desplegar las Specs 1–5 se agregan: novedades y pasos después de actualizar, comportamiento de la POA no vigente en cada sub-pestaña, confirmación al adoptar el físico, panel usado en el MPPT, qué se guarda y cómo se restaura un proyecto multi-superficie, y los mensajes nuevos. El Asistente recibe la misma información sobre guardar y cargar | `08-interfaz`, Asistente |
| 2026-09-24 | Corregir el editor de superficies de Vista 3D: sus campos ya no reciben `value=`/`index=` desde el dato guardado; el estado se inicializa una vez y solo se resincroniza si el dato cambió fuera del campo | Reportado en producción: al cambiar el azimuth (y otros campos) el valor tardaba o volvía al anterior, porque en Streamlit 1.36 la identidad del widget incluye su valor inicial y el campo se recreaba en cada edición. Mismo patrón ya aplicado a los puntos 3D. El encabezado de cada superficie usa ahora los valores actuales. En el mismo cambio, los strings se muestran con un formato único «8 serie × 17 paralelo» (antes «17×8s» en la leyenda y «8 × 17» en la tabla) | `08-interfaz` |
| 2026-09-25 | El mapa de calor de 🌞 Trayectoria Solar indica siempre de dónde salen sus valores: POA vigente de la superficie (nota), POA general de ☀️ Recurso Solar (aviso con el motivo) o estimación fija de 300 W/m² (aviso) | Detectado en la prueba en producción: sin POA vigente, el mapa coloreaba en silencio con la POA general del proyecto bajo el título de la superficie. Completa la Spec `05/vigencia-poa-superficie`, que no había llevado el aviso a esta sub-pestaña | `08-interfaz` |
| 2026-09-25 | Publicar la rev. 5 del manual de Vista 3D con recuadros «💡 Explicación» y el Anexo A (ejercicio completo de verificación con valores) | Recoge las dudas reales de la prueba en producción: texto gris de ejemplo, clic fuera para aplicar, formato de coma decimal, ubicar obstáculos desde el `.json` (mm y `northOffset`), avisos que no bloquean, casilla «Activa», dónde está el banner del origen, bypass sin CSV, lectura de «8 serie × 17 paralelo», sol al norte a mitad de año en Bogotá, fuente de colores del mapa y qué hacer tras un reinicio | `08-interfaz` |
| 2026-09-25 | Cerrar como `completado` las Specs `05/vigencia-poa-superficie`, `05/publicacion-energia-multisuperficie`, `08-interfaz/puntos-3d-validacion`, `08-interfaz/estado-sombra-superficie`, `08-interfaz/panel-proyecto-bypass-mppt` y `08-interfaz/heatmap-poa-superficie` | Integradas a `main` (#43, #45–#48) con CI en verde, desplegadas en Streamlit y verificadas en producción el 24-sep-2026. `05/sombra-cara-trasera` sigue en validación hasta la corrida real de Torre 5 en el Codespace | `05-perdidas-y-temperatura`, `08-interfaz` |
| 2026-09-25 | Aprobar e implementar `05-perdidas-y-temperatura/panel-por-superficie`: cada superficie de Vista 3D declara su panel (por defecto el del proyecto) y la energía simplificada usa η = Pmax / área del módulo de ese panel | Una sola referencia de panel no permitía fachada y techo con paneles distintos, y la energía simplificada leía `eta_panel`, clave que nadie escribe: siempre 16 %. Con `ASP-ST1-T40` (8,75 %) la energía publicada salía 83 % más alta. Quedan fuera y registrados: PR fijo 0,78 (`pr_sistema` vs `PR_sistema`) y Presupuesto sin superficies de Vista 3D | `05-perdidas-y-temperatura`, `08-interfaz`, `03-dimensionamiento` |
